if __name__ == "__main__":
    # TODO: call the general class based on the Argparse (
    # e.g., 'llm_simple' --> from tekboart_llm.apps import llm_simple as llm
    # model = llm.LocalLLM(prompt, MODEL_NMAE (from the pyproject config file), save_path)
    pass
